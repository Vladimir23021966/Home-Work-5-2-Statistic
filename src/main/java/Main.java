public class Main {
    public static void main(String[] args) {

     /*   long[] sales = {8, 15, 13, 15, 17, 20, 19, 20, 7, 14, 14, 18
        };
        // 1. Метод - сумма всех продаж
        StatsService sum = new StatsService();
        long sumAll = sum.sumAllSales(sales);
        System.out.println("Сумма всех продаж -  "+sumAll);

        //2. Метод - Средняя сумма продаж в месяц
        StatsService  average =new StatsService();
        double middle =average.averageSalesAmount(sales);
        System.out.println("Средняя сумма продаж - "+middle);

        // 3. Метод -номер месяца максимальных продаж
        StatsService maxMonth = new StatsService();
        int indexMaxMonth = maxMonth.maxMonthSales(sales);
        System.out.println("Номер месяца максимальных продаж - "+indexMaxMonth);

        // 4. Метод -номер месяца минимальных продаж
        StatsService minMonth = new StatsService();
        int indexMinMonth = minMonth.minMonthSales(sales);
        System.out.println("Номер месяца минимальных продаж - "+indexMinMonth);

        // 5. Метод (Кол-во месяцев, в которых продажи были ниже среднего)
          StatsService indexMin =new StatsService();
          int indexLessMonth=indexMin.monthLessOverage(sales);
        System.out.println("Колличество месяцев с продажами ниже средней суммы продаж - "+indexLessMonth);
        // 6. Метод (Кол-во месяцев, в которых продажи были выше среднего)
         StatsService indexMax =new StatsService();
         int indexMoreMonth=indexMax.monthMoreOverage(sales);
        System.out.println("Колличество месяцев с продажами выше средней суммы продаж - " +indexMoreMonth);

        */

      
    }
}

